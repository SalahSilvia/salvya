/**
 * Third-party analytics bridges (Meta, TikTok, GA4) stay thin; first-party facts live in `tracker.ts` + `/api/analytics/collect`.
 * Add provider modules here as integrations land.
 */
export {};
