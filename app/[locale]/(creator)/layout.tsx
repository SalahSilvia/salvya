/**
 * Creator route group — no storefront navigation.
 * Studio chrome is applied in creator/(studio)/layout.tsx.
 */
export default function CreatorRouteLayout({ children }: { children: React.ReactNode }) {
  return children;
}
